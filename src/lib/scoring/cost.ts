/** Estimated USD cost per GPT-4o mini scoring run (input + output, approximate). */
export const ESTIMATED_SCORE_COST_USD = 0.0025;

export function estimatedScoreCostUsd(): number {
  return ESTIMATED_SCORE_COST_USD;
}
